import { Component } from '@angular/core';
import { AuthManager } from '../../authManager';

@Component({
  selector: 'account-reports',
  template: `<router-outlet></router-outlet>`
})
export class AccountReportComponent {
  constructor() {
  }
}
